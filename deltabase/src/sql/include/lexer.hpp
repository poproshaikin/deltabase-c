#pragma once 

#include "token.hpp"
#include <string>
#include <vector>

namespace sql {
    class Lexer {
        public:
            std::vector<SqlToken> tokenize(std::string command) {
                
            }
    };
}
