#pragma once

namespace journal {
    struct LogEntry {

    };

    class Journal {
        // 1) создать структуру с обозначением типа операции, времени и аргументами
        // 2) базовый CRUD для работы с записями
        // 3) исполнитель действий обозначенных в записях, который уже и будет вызывать функции ядра
    };
}